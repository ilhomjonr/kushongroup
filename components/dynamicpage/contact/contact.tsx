import styles from "./contact.module.css";

export const DynamicPageContacts = () => {
  return (
    <section>
      <div className={`dynamic_box ${styles.contact_inner}`}>Contact</div>
    </section>
  );
};
