import styles from "./projects.module.css";

export const DynamicPageProjects = () => {
  return (
    <section>
      <div className={`dynamic_box ${styles.projects_inner}`}>Projects</div>
    </section>
  );
};
